# create helper.csv

# ssyr <- 1:328
# yrc <- seq(1947.125, 2028.875, by = 0.25)
# year <- floor(yrc)# the integer year
# ss <- rep(1:4, length.out = length(ssyr))# the season in ss3
#
# helper <- data.frame(ssyr = ssyr, yrc = yrc, year = year, ss = ss)
#
# write.csv(helper, file = "inst/extdata/helper.csv", row.names = FALSE)

